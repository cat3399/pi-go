package app

// auth.json parsing and credential-source resolution moved to internal/auth.
// This tracked file remains intentionally to avoid a deletion-only migration
// diff; it contains no second auth implementation.
