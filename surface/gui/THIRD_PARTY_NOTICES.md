# Third-party notices

The desktop product bundles `@cat3399/pi-workbench`. Its OpenCodex-derived
source scope and AGPL-3.0-only license are recorded in
`../ui/THIRD_PARTY_NOTICES.md` and `../ui/LICENSE`.

The native host uses Wails v3 (MIT). The frontend build also uses React (MIT),
Lucide (ISC), and Vite (MIT). Exact Go and npm versions are locked in `go.sum`
and `frontend/package-lock.json`; the corresponding upstream license files
must be included in distributable packaging.

No compiled official Codex/OpenCodex renderer or extracted application bundle
is part of this product.
