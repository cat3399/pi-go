# Third-party notices

The Android product bundles `@cat3399/pi-workbench`. Its OpenCodex-derived
source scope and AGPL-3.0-only license are recorded in
`../ui/THIRD_PARTY_NOTICES.md` and `../ui/LICENSE`.

The native host and checked-in Android host template use Wails v3 (MIT). The
Android build uses Gradle and AndroidX/Material components under their
respective upstream licenses. The frontend uses React (MIT), Lucide (ISC), and
Vite (MIT). Exact Go, npm, and Gradle versions are locked in `go.sum`,
`frontend/package-lock.json`, and `build/android/gradle/wrapper`.

No compiled official Codex/OpenCodex renderer, extracted application bundle,
minified third-party renderer, or encrypted UI implementation is included.
